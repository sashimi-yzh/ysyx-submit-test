From 94528d9bf931637ec0f1f17d4cef9734ac191e55 Mon Sep 17 00:00:00 2001
From: 24011211232-Deng Xidian <1447762756@qq.com>
Date: Sun, 8 Mar 2026 13:50:03 +0800
Subject: [PATCH 12/27] commit260308

---
 csrc/include/npc_counter.h |  4 ++--
 csrc/src/npc-exec.cpp      | 16 ++++++-------
 csrc/src/npc_counter.cpp   | 49 ++++++++++++++++----------------------
 3 files changed, 31 insertions(+), 38 deletions(-)

diff --git a/csrc/include/npc_counter.h b/csrc/include/npc_counter.h
index 52506c34..8613dbda 100644
--- a/csrc/include/npc_counter.h
+++ b/csrc/include/npc_counter.h
@@ -3,9 +3,9 @@
 
 #include <common.h>
 
-void cycle_add(int type, uint32_t num);
+// void cycle_add(int type, uint32_t num);
 
-int type_of_inst();
+// int type_of_inst();
 
 void display_counter();
 
diff --git a/csrc/src/npc-exec.cpp b/csrc/src/npc-exec.cpp
index 51b8e755..53e44994 100644
--- a/csrc/src/npc-exec.cpp
+++ b/csrc/src/npc-exec.cpp
@@ -281,14 +281,14 @@ static void npc_once(){
   // }
 
 
-  if(inst_pre == inst_now){
-    abort_count ++;
-  }
-  else{
-    int type = type_of_inst();
-    cycle_add(type, abort_count);
-    abort_count = 0;
-  }
+  // if(inst_pre == inst_now){
+  //   abort_count ++;
+  // }
+  // else{
+  //   int type = type_of_inst();
+  //   cycle_add(type, abort_count);
+  //   abort_count = 0;
+  // }
   
   step_and_dump_wave();  // 下降沿
 
diff --git a/csrc/src/npc_counter.cpp b/csrc/src/npc_counter.cpp
index 2fd677ab..d264e776 100644
--- a/csrc/src/npc_counter.cpp
+++ b/csrc/src/npc_counter.cpp
@@ -13,54 +13,48 @@ static uint64_t icache_hit = 0;
 // static int access_time = 3;
 // static int miss_penalty = 37;
 
-enum {LUI, AUIPC, ALU, LSU, JAL, BRANCH, CSR, NO_TYPE};
+enum {IFU, LSU, XBAR, RAW, BRANCH, NO_TYPE};
 
-static int inst_type = NO_TYPE;
+// static int inst_type = NO_TYPE;
 
 static struct counter {
     const char *regex;
-    uint64_t inst_count;
-    uint64_t cycle_count;
+    uint64_t time_count; 
+    uint64_t cycle_count; 
 } counters[] = {
-    {"lui_inst", 0, 0},
-    {"auipc_inst", 0, 0},
-    {"alu_inst", 0, 0},       
-    {"lsu_inst", 0, 0},
-    {"jal_inst", 0, 0},
-    {"branch_inst", 0, 0},
-    {"csr_inst", 0, 0},
-    {"notype", 0, 0},
+    {"ifu_delay", 0, 0},
+    {"lsu_delay", 0, 0},
+    {"xbar_delay", 0, 0},
+    {"raw_delay", 0, 0},
+    {"branch_delay", 0, 0},
 };
 
 #ifdef CONFIG_COUNTER
-extern "C" void inst_add(int no){
-    counters[no].inst_count ++;
-    inst_type = no;
+extern "C" void time_add(int no){
+    counters[no].time_count ++;
     return;
 }
+
+extern "C" void cycle_add(int no){
+    counters[no].cycle_count ++;
+}
+
 void display_counter(){
 
     printf("           type |   num    | cycles/inst\n");
 
-    for(int i = 0; i < 6; i++){
-        if(counters[i].inst_count != 0){
-            printf("%15s | %8ld | %ld\n", counters[i].regex, counters[i].inst_count, counters[i].cycle_count/counters[i].inst_count);
+    for(int i = 0; i < 5; i++){
+        if(counters[i].time_count != 0){
+            printf("%15s | %8ld | %ld\n", counters[i].regex, counters[i].time_count, counters[i].cycle_count/counters[i].time_count);
         }
     }
 }
 #else
-extern "C" void inst_add(int no){ }
+extern "C" void time_add(int no){ }
 void display_counter(){ }
+extern "C" void cycle_add(int type, uint32_t num){ }
 #endif
 
-void cycle_add(int type, uint32_t num){
-    counters[type].cycle_count += num;
-}
-
-int type_of_inst(){
-    return inst_type;
-}
-
 #ifdef CONFIG_AMAT
 extern "C" void icache_add(int no){
     if(no == 0){
@@ -72,7 +66,6 @@ extern "C" void icache_add(int no){
 }
 void display_amat(){
     if(icache_total !=0 ){ p_hit = (float)icache_hit / (float)icache_total; }
-    // amat = (float)access_time + (1 - p_hit) * (float)miss_penalty;
     printf("AMAT: total = %ld, hit = %ld, p_hit = %.2f\n", icache_total, icache_hit, p_hit);
 }
 #else 
-- 
2.34.1

