From 90866ccc64a48208bc71ee57a7f600ceb3e10b6b Mon Sep 17 00:00:00 2001
From: Wang Yi Jie <miwyj@mail.scut.edu.cn>
Date: Sun, 25 Jan 2026 15:11:27 +0800
Subject: [PATCH 12/28] impl bit extend sdram

---
 perip/sdram/core_sdram_axi4/sdram_axi.v      |   4 +
 perip/sdram/core_sdram_axi4/sdram_axi_core.v | 125 ++++++++++---------
 perip/sdram/core_sdram_axi4/sdram_axi_pmem.v |   4 +
 perip/sdram/sdram.v                          |   4 +
 perip/sdram/sdram_top_apb.v                  |  14 ++-
 perip/sdram/sdram_top_axi.v                  |   4 +
 src/device/SDRAM.scala                       |  91 ++++++++++----
 src/device/SPI.scala                         |  14 ++-
 8 files changed, 171 insertions(+), 89 deletions(-)

diff --git a/perip/sdram/core_sdram_axi4/sdram_axi.v b/perip/sdram/core_sdram_axi4/sdram_axi.v
index 64641f58..d8fe4eda 100644
--- a/perip/sdram/core_sdram_axi4/sdram_axi.v
+++ b/perip/sdram/core_sdram_axi4/sdram_axi.v
@@ -1,3 +1,5 @@
+/*verilator public_on*/
+/*verilator public_on*/
 //-----------------------------------------------------------------
 //                    SDRAM Controller (AXI4)
 //                           V1.0
@@ -192,3 +194,5 @@ u_core
 
 
 endmodule
+/*verilator public_off*/
+/*verilator public_off*/
diff --git a/perip/sdram/core_sdram_axi4/sdram_axi_core.v b/perip/sdram/core_sdram_axi4/sdram_axi_core.v
index cedbf77b..5443bc10 100644
--- a/perip/sdram/core_sdram_axi4/sdram_axi_core.v
+++ b/perip/sdram/core_sdram_axi4/sdram_axi_core.v
@@ -1,3 +1,5 @@
+/*verilator public_on*/
+/*verilator public_on*/
 //-----------------------------------------------------------------
 //                    SDRAM Controller (AXI4)
 //                           V1.0
@@ -42,7 +44,7 @@ module sdram_axi_core
     ,input  [  7:0]  inport_len_i
     ,input  [ 31:0]  inport_addr_i
     ,input  [ 31:0]  inport_write_data_i
-    ,input  [ 15:0]  sdram_data_input_i
+    ,input  [ 31:0]  sdram_data_input_i
 
     // Outputs
     ,output          inport_accept_o
@@ -55,10 +57,10 @@ module sdram_axi_core
     ,output          sdram_ras_o
     ,output          sdram_cas_o
     ,output          sdram_we_o
-    ,output [  1:0]  sdram_dqm_o
+    ,output [  3:0]  sdram_dqm_o
     ,output [ 12:0]  sdram_addr_o
     ,output [  1:0]  sdram_ba_o
-    ,output [ 15:0]  sdram_data_output_o
+    ,output [ 31:0]  sdram_data_output_o
     ,output          sdram_data_out_en_o
 );
 
@@ -70,13 +72,16 @@ module sdram_axi_core
 parameter SDRAM_MHZ              = 50;
 parameter SDRAM_ADDR_W           = 24;
 parameter SDRAM_COL_W            = 9;
+// parameter SDRAM_READ_LATENCY     = 1;
+// NEED modified by passing it from user module!!!
 parameter SDRAM_READ_LATENCY     = 2;
 
 //-----------------------------------------------------------------
 // Defines / Local params
 //-----------------------------------------------------------------
 localparam SDRAM_BANK_W          = 2;
-localparam SDRAM_DQM_W           = 2;
+// localparam SDRAM_DQM_W           = 2;
+localparam SDRAM_DQM_W           = 4;
 localparam SDRAM_BANKS           = 2 ** SDRAM_BANK_W;
 localparam SDRAM_ROW_W           = SDRAM_ADDR_W - SDRAM_COL_W - SDRAM_BANK_W;
 localparam SDRAM_REFRESH_CNT     = 2 ** SDRAM_ROW_W;
@@ -93,8 +98,9 @@ localparam CMD_PRECHARGE     = 4'b0010;
 localparam CMD_REFRESH       = 4'b0001;
 localparam CMD_LOAD_MODE     = 4'b0000;
 
-// Mode: Burst Length = 4 bytes, CAS=2
-localparam MODE_REG          = {3'b000,1'b0,2'b00,3'b010,1'b0,3'b001};
+// // Mode: Burst Length = 4 bytes, CAS=2
+// Burst Length = 1
+localparam MODE_REG          = {3'b000,1'b0,2'b00,3'b010,1'b0,3'b000};
 
 // SM states
 localparam STATE_W           = 4;
@@ -105,14 +111,14 @@ localparam STATE_ACTIVATE    = 4'd3;
 localparam STATE_READ        = 4'd4;
 localparam STATE_READ_WAIT   = 4'd5;
 localparam STATE_WRITE0      = 4'd6;
-localparam STATE_WRITE1      = 4'd7;
+// localparam STATE_WRITE1      = 4'd7;
 localparam STATE_PRECHARGE   = 4'd8;
 localparam STATE_REFRESH     = 4'd9;
 
 localparam AUTO_PRECHARGE    = 10;
 localparam ALL_BANKS         = 10;
 
-localparam SDRAM_DATA_W      = 16;
+localparam SDRAM_DATA_W      = 32;
 
 localparam CYCLE_TIME_NS     = 1000 / SDRAM_MHZ;
 
@@ -271,36 +277,37 @@ begin
     begin
         next_state_r = STATE_IDLE;
 
-        // Another pending read request (with no refresh pending)
-        if (!refresh_q && ram_req_w && ram_rd_w)
-        begin
-            // Open row hit
-            if (row_open_q[addr_bank_w] && addr_row_w == active_row_q[addr_bank_w])
-                next_state_r = STATE_READ;
-        end
+        // // Another pending read request (with no refresh pending)
+        // if (!refresh_q && ram_req_w && ram_rd_w)
+        // begin
+        //     // Open row hit
+        //     if (row_open_q[addr_bank_w] && addr_row_w == active_row_q[addr_bank_w])
+        //         next_state_r = STATE_READ;
+        // end
     end
     //-----------------------------------------
     // STATE_WRITE0
     //-----------------------------------------
     STATE_WRITE0 :
     begin
-        next_state_r = STATE_WRITE1;
-    end
-    //-----------------------------------------
-    // STATE_WRITE1
-    //-----------------------------------------
-    STATE_WRITE1 :
-    begin
+        // next_state_r = STATE_WRITE1;
         next_state_r = STATE_IDLE;
-
-        // Another pending write request (with no refresh pending)
-        if (!refresh_q && ram_req_w && (ram_wr_w != 4'b0))
-        begin
-            // Open row hit
-            if (row_open_q[addr_bank_w] && addr_row_w == active_row_q[addr_bank_w])
-                next_state_r = STATE_WRITE0;
-        end
     end
+    // //-----------------------------------------
+    // // STATE_WRITE1
+    // //-----------------------------------------
+    // STATE_WRITE1 :
+    // begin
+    //     next_state_r = STATE_IDLE;
+    //
+    //     // Another pending write request (with no refresh pending)
+    //     if (!refresh_q && ram_req_w && (ram_wr_w != 4'b0))
+    //     begin
+    //         // Open row hit
+    //         if (row_open_q[addr_bank_w] && addr_row_w == active_row_q[addr_bank_w])
+    //             next_state_r = STATE_WRITE0;
+    //     end
+    // end
     //-----------------------------------------
     // STATE_PRECHARGE
     //-----------------------------------------
@@ -483,7 +490,7 @@ always @ (posedge clk_i or posedge rst_i)
 if (rst_i)
 begin
     command_q       <= CMD_NOP;
-    data_q          <= 16'b0;
+    data_q          <= 32'b0;
     addr_q          <= {SDRAM_ROW_W{1'b0}};
     bank_q          <= {SDRAM_BANK_W{1'b0}};
     cke_q           <= 1'b0;
@@ -616,33 +623,35 @@ begin
         command_q       <= CMD_WRITE;
         addr_q          <= addr_col_w;
         bank_q          <= addr_bank_w;
-        data_q          <= ram_write_data_w[15:0];
+        data_q          <= ram_write_data_w[31:0];
 
         // Disable auto precharge (auto close of row)
         addr_q[AUTO_PRECHARGE]  <= 1'b0;
 
         // Write mask
-        dqm_q           <= ~ram_wr_w[1:0];
-        dqm_buffer_q    <= ~ram_wr_w[3:2];
+        dqm_q           <= ~ram_wr_w[3:0];
+        dqm_buffer_q    <= ~ram_wr_w[3:0];
+        // dqm_q           <= ~ram_wr_w[1:0];
+        // dqm_buffer_q    <= ~ram_wr_w[3:2];
 
         data_rd_en_q    <= 1'b0;
     end
-    //-----------------------------------------
-    // STATE_WRITE1
-    //-----------------------------------------
-    STATE_WRITE1 :
-    begin
-        // Burst continuation
-        command_q   <= CMD_NOP;
-
-        data_q      <= data_buffer_q;
-
-        // Disable auto precharge (auto close of row)
-        addr_q[AUTO_PRECHARGE]  <= 1'b0;
-
-        // Write mask
-        dqm_q       <= dqm_buffer_q;
-    end
+    // //-----------------------------------------
+    // // STATE_WRITE1
+    // //-----------------------------------------
+    // STATE_WRITE1 :
+    // begin
+    //     // Burst continuation
+    //     command_q   <= CMD_NOP;
+    //
+    //     data_q      <= data_buffer_q;
+    //
+    //     // Disable auto precharge (auto close of row)
+    //     addr_q[AUTO_PRECHARGE]  <= 1'b0;
+    //
+    //     // Write mask
+    //     dqm_q       <= dqm_buffer_q;
+    // end
     endcase
 end
 
@@ -665,14 +674,14 @@ else
 // in WRITE0. Also buffer lower 16-bits of read data.
 always @ (posedge clk_i or posedge rst_i)
 if (rst_i)
-    data_buffer_q <= 16'b0;
-else if (state_q == STATE_WRITE0)
-    data_buffer_q <= ram_write_data_w[31:16];
+    data_buffer_q <= 32'b0;
+// else if (state_q == STATE_WRITE0)
+//     data_buffer_q <= ram_write_data_w[31:16];
 else if (rd_q[SDRAM_READ_LATENCY+1])
     data_buffer_q <= sample_data_q;
 
 // Read data output
-assign ram_read_data_w = {sample_data_q, data_buffer_q};
+assign ram_read_data_w = {sample_data_q}; //, data_buffer_q};
 
 //-----------------------------------------------------------------
 // ACK
@@ -684,7 +693,9 @@ if (rst_i)
     ack_q   <= 1'b0;
 else
 begin
-    if (state_q == STATE_WRITE1)
+    // if (state_q == STATE_WRITE1)
+    //     ack_q <= 1'b1;
+    if (state_q == STATE_WRITE0)
         ack_q <= 1'b1;
     else if (rd_q[SDRAM_READ_LATENCY+1])
         ack_q <= 1'b1;
@@ -730,7 +741,7 @@ begin
     STATE_READ        : dbg_state = "READ";
     STATE_READ_WAIT   : dbg_state = "READ_WAIT";
     STATE_WRITE0      : dbg_state = "WRITE0";
-    STATE_WRITE1      : dbg_state = "WRITE1";
+    // STATE_WRITE1      : dbg_state = "WRITE1";
     STATE_PRECHARGE   : dbg_state = "PRECHARGE";
     STATE_REFRESH     : dbg_state = "REFRESH";
     default           : dbg_state = "UNKNOWN";
@@ -740,3 +751,5 @@ end
 
 
 endmodule
+/*verilator public_off*/
+/*verilator public_off*/
diff --git a/perip/sdram/core_sdram_axi4/sdram_axi_pmem.v b/perip/sdram/core_sdram_axi4/sdram_axi_pmem.v
index 3d6a5bb4..d135001b 100644
--- a/perip/sdram/core_sdram_axi4/sdram_axi_pmem.v
+++ b/perip/sdram/core_sdram_axi4/sdram_axi_pmem.v
@@ -1,3 +1,5 @@
+/*verilator public_on*/
+/*verilator public_on*/
 //-----------------------------------------------------------------
 //                    SDRAM Controller (AXI4)
 //                           V1.0
@@ -426,3 +428,5 @@ assign data_out_o = ram[rd_ptr];
 
 
 endmodule
+/*verilator public_off*/
+/*verilator public_off*/
diff --git a/perip/sdram/sdram.v b/perip/sdram/sdram.v
index 3613b231..00918567 100644
--- a/perip/sdram/sdram.v
+++ b/perip/sdram/sdram.v
@@ -1,3 +1,5 @@
+/*verilator public_on*/
+/*verilator public_on*/
 module sdram(
   input        clk,
   input        cke,
@@ -14,3 +16,5 @@ module sdram(
   assign dq = 16'bz;
 
 endmodule
+/*verilator public_off*/
+/*verilator public_off*/
diff --git a/perip/sdram/sdram_top_apb.v b/perip/sdram/sdram_top_apb.v
index 3bc5b93b..5fb16c8f 100644
--- a/perip/sdram/sdram_top_apb.v
+++ b/perip/sdram/sdram_top_apb.v
@@ -1,3 +1,5 @@
+/*verilator public_on*/
+/*verilator public_on*/
 module sdram_top_apb (
   input         clock,
   input         reset,
@@ -20,13 +22,13 @@ module sdram_top_apb (
   output        sdram_we,
   output [12:0] sdram_a,
   output [ 1:0] sdram_ba,
-  output [ 1:0] sdram_dqm,
-  inout  [15:0] sdram_dq
+  output [ 3:0] sdram_dqm,
+  inout  [31:0] sdram_dq
 );
 
   wire sdram_dout_en;
-  wire [15:0] sdram_dout;
-  assign sdram_dq = sdram_dout_en ? sdram_dout : 16'bz;
+  wire [31:0] sdram_dout;
+  assign sdram_dq = sdram_dout_en ? sdram_dout : 32'bz;
 
   typedef enum [1:0] { ST_IDLE, ST_WAIT_ACCEPT, ST_WAIT_ACK } state_t;
   reg [1:0] state;
@@ -49,7 +51,7 @@ module sdram_top_apb (
     .SDRAM_MHZ(100),
     .SDRAM_ADDR_W(24),
     .SDRAM_COL_W(9),
-    .SDRAM_READ_LATENCY(2)
+    .SDRAM_READ_LATENCY(1)
   ) u_sdram_ctrl(
     .clk_i(clock),
     .rst_i(reset),
@@ -78,3 +80,5 @@ module sdram_top_apb (
   );
 
 endmodule
+/*verilator public_off*/
+/*verilator public_off*/
diff --git a/perip/sdram/sdram_top_axi.v b/perip/sdram/sdram_top_axi.v
index f0fd6fdb..0208f1bd 100644
--- a/perip/sdram/sdram_top_axi.v
+++ b/perip/sdram/sdram_top_axi.v
@@ -1,3 +1,5 @@
+/*verilator public_on*/
+/*verilator public_on*/
 module sdram_top_axi(
   input         clock,
   input         reset,
@@ -97,3 +99,5 @@ module sdram_top_axi(
   );
 
 endmodule
+/*verilator public_off*/
+/*verilator public_off*/
diff --git a/src/device/SDRAM.scala b/src/device/SDRAM.scala
index f867af6f..e3da13e8 100644
--- a/src/device/SDRAM.scala
+++ b/src/device/SDRAM.scala
@@ -3,6 +3,7 @@ package ysyx
 import chisel3._
 import chisel3.util._
 import chisel3.experimental.Analog
+import chisel3.experimental.attach
 
 import freechips.rocketchip.amba.axi4._
 import freechips.rocketchip.amba.apb._
@@ -11,7 +12,7 @@ import freechips.rocketchip.diplomacy._
 import freechips.rocketchip.util._
 import chisel3.util.circt.dpi._
 
-class SDRAMIO extends Bundle {
+class SDRAMIO(dqWidth:Width=32.W) extends Bundle {
   val clk = Output(Bool())
   val cke = Output(Bool())
   val cs = Output(Bool())
@@ -20,8 +21,8 @@ class SDRAMIO extends Bundle {
   val we = Output(Bool())
   val a = Output(UInt(13.W))
   val ba = Output(UInt(2.W))
-  val dqm = Output(UInt(2.W))
-  val dq = Analog(16.W)
+  val dqm = Output(UInt(4.W))
+  val dq = Analog(dqWidth)
 }
 
 class sdram_top_axi extends BlackBox {
@@ -51,16 +52,72 @@ class sdram extends BlackBox {
   val io = IO(Flipped(new SDRAMIO))
 }
 
+class sdramChisel extends RawModule {
+  val io = IO(Flipped(new SDRAMIO(32.W)))
+  dontTouch(io)
+  val block0 = Module(new sdramChiselBlock(0))
+  val block1 = Module(new sdramChiselBlock(1))
+
+  val dout = WireDefault(0.U(32.W))
+  val dout_en = WireDefault(false.B)
+  val di = TriStateInBuf(io.dq, dout, dout_en)
+
+  block0.io.clk := io.clk
+  block0.io.cke := io.cke
+  block0.io.cs := io.cs
+  block0.io.ras := io.ras
+  block0.io.cas := io.cas
+  block0.io.we := io.we
+  block0.io.a := io.a
+  block0.io.ba := io.ba
+  block0.io.dqm := io.dqm(1,0)
+
+  block0.dataio.din := di(15,0)
+  val block0_dout = block0.dataio.dout
+  val block0_dout_en = block0.dataio.dout_en
+
+  block1.io.clk := io.clk
+  block1.io.cke := io.cke
+  block1.io.cs := io.cs
+  block1.io.ras := io.ras
+  block1.io.cas := io.cas
+  block1.io.we := io.we
+  block1.io.a := io.a
+  block1.io.ba := io.ba
+  block1.io.dqm := io.dqm(3,2)
+
+  block1.dataio.din := di(31,16)
+  val block1_dout = block1.dataio.dout
+  val block1_dout_en = block1.dataio.dout_en
+
+  dout := Cat(block1_dout, block0_dout)
+  dout_en := block0_dout_en || block1_dout_en
+}
+
 // MT48LC16M16A2
 //  16 Meg x 16
 // Row addressing: 8K A[12:0]
 // Bank addressing: 4 B[1:0]
 // Column addressing: 512 A[8:0]
-class sdramChisel extends RawModule {
-  val io = IO(Flipped(new SDRAMIO))
+//
+// one block dq: 16 bits
+//
+class sdramChiselBlock(block:Int=0) extends RawModule {
+  val io = IO(Flipped(new SDRAMIO(16.W)))
+
+  val dataio = IO(new Bundle {
+    val dout = Output(UInt(16.W))
+    val dout_en = Output(Bool())
+    val din = Input(UInt(16.W))
+  })
+
   val dout = WireDefault(0.U(16.W))
   val dout_en = WireDefault(false.B)
-  val di = TriStateInBuf(io.dq, dout, dout_en)
+  // val di = TriStateInBuf(io.dq, dout, dout_en)
+
+  val di = dataio.din
+  dataio.dout := dout
+  dataio.dout_en := dout_en
 
   dontTouch(io)
 
@@ -114,23 +171,11 @@ class sdramChisel extends RawModule {
     val enReadCall = WireDefault(false.B)
     dontTouch(enReadCall)
 
-    // val rdataReg = Wire(UInt(16.W))
-    // dontTouch(rdataReg)
-    // when(enReadCall){
-    //   rdataReg := MuxLookup(col, 0.U)(Seq(
-    //     0.U -> "h1234".U(16.W),
-    //     1.U -> "h5678".U(16.W),
-    //     2.U -> "habcd".U(16.W),
-    //     3.U -> "hef01".U(16.W)
-    //   ))
-    // } .otherwise {
-    //   rdataReg := 0.U(16.W)
-    // }
     val rData = RawUnclockedNonVoidFunctionCall(
       "sdram_read",
       UInt(16.W),
-      Some(Seq("bank", "row", "col"))
-    )(enReadCall, bank.pad(8), row(bank).pad(16), col.pad(16))
+      Some(Seq("block","bank", "row", "col"))
+    )(enReadCall, block.U(8.W),bank.pad(8), row(bank).pad(16), col.pad(16))
 
     val wData = WireDefault(InvalidValue)
     val enWriteCall = WireDefault(false.B)
@@ -150,10 +195,11 @@ class sdramChisel extends RawModule {
     dontTouch(enWriteCall)
     RawClockedVoidFunctionCall(
       "sdram_write",
-      Some(Seq("bank", "row", "col", "data", "mask"))
+      Some(Seq("block","bank", "row", "col", "data", "mask"))
     )(
       io.clk.asClock,
       enWriteCall,
+      block.U(8.W),
       wBank.pad(8),
       row(wBank).pad(16),
       wCol,
@@ -167,7 +213,7 @@ class sdramChisel extends RawModule {
       state := State.casWait
       casCount := 0.U
     }.elsewhen(cmd === CMD.WRITE) {
-      state := State.recvWData
+      state := Mux(burstLength === 1.U, State.idle, State.recvWData)
       wData := di
       enWriteCall := true.B
     }.elsewhen(state === State.recvWData) {
@@ -195,6 +241,7 @@ class sdramChisel extends RawModule {
   }
 }
 
+
 class AXI4SDRAM(address: Seq[AddressSet])(implicit p: Parameters)
     extends LazyModule {
   val beatBytes = 4
diff --git a/src/device/SPI.scala b/src/device/SPI.scala
index c106ef0e..e30997ad 100644
--- a/src/device/SPI.scala
+++ b/src/device/SPI.scala
@@ -119,9 +119,11 @@ class APBSPI(address: Seq[AddressSet])(implicit p: Parameters)
     val mspiReady = mspi.io.in.pready
     val mspiRData = mspi.io.in.prdata
 
-    when(in.paddr >= 0x30000000.U && in.paddr < 0x40000000.U){
-      mspi.io.in.psel := (!isIdle)&&(!isDone)&&(!currentCheckBusy)
-      mspi.io.in.penable := (~isSetupStage)&&(!currentCheckBusy)
+    val enFastFlash = sys.env.getOrElse("SOC_USE_FAST_FLASH", "false").toBoolean
+
+    when(in.paddr >= 0x30000000.U && in.paddr < 0x40000000.U && (!enFastFlash).B) {
+      mspi.io.in.psel := (!isIdle) && (!isDone) && (!currentCheckBusy)
+      mspi.io.in.penable := (~isSetupStage) && (!currentCheckBusy)
       mspi.io.in.paddr := currentAddr
       mspi.io.in.pwrite := currentIsWrite
       mspi.io.in.pwdata := currentWriteData
@@ -156,11 +158,11 @@ class APBSPI(address: Seq[AddressSet])(implicit p: Parameters)
             }
             step := step + 1.U
             isSetupStage := true.B
-          }.otherwise{
+          }.otherwise {
             // Interrupt signal is deasserted after a Read or Write to any register.
             // so only when isCheckBusy need goto next step
-            when(mspi.io.spi_irq_out && currentCheckBusy){
-              step:=step+1.U
+            when(mspi.io.spi_irq_out && currentCheckBusy) {
+              step := step + 1.U
               isSetupStage := true.B
             }
           }
-- 
2.43.0

