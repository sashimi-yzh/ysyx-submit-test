From efa8d483cdcc7050f48980b75ae693f69eb20cda Mon Sep 17 00:00:00 2001
From: Zihao Yu <yuzihao@ict.ac.cn>
Date: Sun, 31 Mar 2024 14:34:05 +0800
Subject: [PATCH 06/13] perip,sdram: extend word size to 32 bit

---
 perip/sdram/core_sdram_axi4/sdram_axi.v      |  6 ++
 perip/sdram/core_sdram_axi4/sdram_axi_core.v | 91 +++++++++++++++-----
 perip/sdram/sdram_top_apb.v                  |  9 +-
 perip/sdram/sdram_top_axi.v                  |  9 +-
 src/SoC.scala                                |  2 +-
 src/device/SDRAM.scala                       | 42 ++++++---
 6 files changed, 123 insertions(+), 36 deletions(-)

diff --git a/perip/sdram/core_sdram_axi4/sdram_axi.v b/perip/sdram/core_sdram_axi4/sdram_axi.v
index 64641f58..769048ea 100644
--- a/perip/sdram/core_sdram_axi4/sdram_axi.v
+++ b/perip/sdram/core_sdram_axi4/sdram_axi.v
@@ -54,6 +54,7 @@ module sdram_axi
     ,input  [  1:0]  inport_arburst_i
     ,input           inport_rready_i
     ,input  [ 15:0]  sdram_data_input_i
+    ,input  [ 15:0]  sdram_data2_input_i
 
     // Outputs
     ,output          inport_awready_o
@@ -74,9 +75,11 @@ module sdram_axi
     ,output          sdram_cas_o
     ,output          sdram_we_o
     ,output [  1:0]  sdram_dqm_o
+    ,output [  1:0]  sdram_dqm2_o
     ,output [ 12:0]  sdram_addr_o
     ,output [  1:0]  sdram_ba_o
     ,output [ 15:0]  sdram_data_output_o
+    ,output [ 15:0]  sdram_data2_output_o
     ,output          sdram_data_out_en_o
 );
 
@@ -182,11 +185,14 @@ u_core
     ,.sdram_cas_o(sdram_cas_o)
     ,.sdram_we_o(sdram_we_o)
     ,.sdram_dqm_o(sdram_dqm_o)
+    ,.sdram_dqm2_o(sdram_dqm2_o)
     ,.sdram_addr_o(sdram_addr_o)
     ,.sdram_ba_o(sdram_ba_o)
     ,.sdram_data_output_o(sdram_data_output_o)
+    ,.sdram_data2_output_o(sdram_data2_output_o)
     ,.sdram_data_out_en_o(sdram_data_out_en_o)
     ,.sdram_data_input_i(sdram_data_input_i)
+    ,.sdram_data2_input_i(sdram_data2_input_i)
 );
 
 
diff --git a/perip/sdram/core_sdram_axi4/sdram_axi_core.v b/perip/sdram/core_sdram_axi4/sdram_axi_core.v
index cedbf77b..f4db235d 100644
--- a/perip/sdram/core_sdram_axi4/sdram_axi_core.v
+++ b/perip/sdram/core_sdram_axi4/sdram_axi_core.v
@@ -43,6 +43,7 @@ module sdram_axi_core
     ,input  [ 31:0]  inport_addr_i
     ,input  [ 31:0]  inport_write_data_i
     ,input  [ 15:0]  sdram_data_input_i
+    ,input  [ 15:0]  sdram_data2_input_i
 
     // Outputs
     ,output          inport_accept_o
@@ -56,9 +57,11 @@ module sdram_axi_core
     ,output          sdram_cas_o
     ,output          sdram_we_o
     ,output [  1:0]  sdram_dqm_o
+    ,output [  1:0]  sdram_dqm2_o
     ,output [ 12:0]  sdram_addr_o
     ,output [  1:0]  sdram_ba_o
     ,output [ 15:0]  sdram_data_output_o
+    ,output [ 15:0]  sdram_data2_output_o
     ,output          sdram_data_out_en_o
 );
 
@@ -94,7 +97,9 @@ localparam CMD_REFRESH       = 4'b0001;
 localparam CMD_LOAD_MODE     = 4'b0000;
 
 // Mode: Burst Length = 4 bytes, CAS=2
-localparam MODE_REG          = {3'b000,1'b0,2'b00,3'b010,1'b0,3'b001};
+//localparam MODE_REG          = {3'b000,1'b0,2'b00,3'b010,1'b0,3'b001};
+// Mode: Burst Length = 2 bytes, CAS=2
+localparam MODE_REG          = {3'b000,1'b0,2'b00,3'b010,1'b0,3'b000};
 
 // SM states
 localparam STATE_W           = 4;
@@ -154,8 +159,10 @@ assign inport_accept_o    = ram_accept_w;
 reg [CMD_W-1:0]        command_q;
 reg [SDRAM_ROW_W-1:0]  addr_q;
 reg [SDRAM_DATA_W-1:0] data_q;
+reg [SDRAM_DATA_W-1:0] data2_q;
 reg                    data_rd_en_q;
 reg [SDRAM_DQM_W-1:0]  dqm_q;
+reg [SDRAM_DQM_W-1:0]  dqm2_q;
 reg                    cke_q;
 reg [SDRAM_BANK_W-1:0] bank_q;
 
@@ -164,6 +171,7 @@ reg [SDRAM_DATA_W-1:0] data_buffer_q;
 reg [SDRAM_DQM_W-1:0]  dqm_buffer_q;
 
 wire [SDRAM_DATA_W-1:0] sdram_data_in_w;
+wire [SDRAM_DATA_W-1:0] sdram_data2_in_w;
 
 reg                    refresh_q;
 
@@ -177,9 +185,12 @@ reg  [STATE_W-1:0]     target_state_q;
 reg  [STATE_W-1:0]     delay_state_q;
 
 // Address bits
-wire [SDRAM_ROW_W-1:0]  addr_col_w  = {{(SDRAM_ROW_W-SDRAM_COL_W){1'b0}}, ram_addr_w[SDRAM_COL_W:2], 1'b0};
-wire [SDRAM_ROW_W-1:0]  addr_row_w  = ram_addr_w[SDRAM_ADDR_W:SDRAM_COL_W+2+1];
-wire [SDRAM_BANK_W-1:0] addr_bank_w = ram_addr_w[SDRAM_COL_W+2:SDRAM_COL_W+2-1];
+//wire [SDRAM_ROW_W-1:0]  addr_col_w  = {{(SDRAM_ROW_W-SDRAM_COL_W){1'b0}}, ram_addr_w[SDRAM_COL_W:2], 1'b0};
+//wire [SDRAM_ROW_W-1:0]  addr_row_w  = ram_addr_w[SDRAM_ADDR_W:SDRAM_COL_W+2+1];
+//wire [SDRAM_BANK_W-1:0] addr_bank_w = ram_addr_w[SDRAM_COL_W+2:SDRAM_COL_W+2-1];
+wire [SDRAM_ROW_W-1:0]  addr_col_w  = {{(SDRAM_ROW_W-SDRAM_COL_W){1'b0}}, ram_addr_w[SDRAM_COL_W+1:2]};
+wire [SDRAM_ROW_W-1:0]  addr_row_w  = ram_addr_w[SDRAM_ROW_W+SDRAM_COL_W+4-1:SDRAM_COL_W+4];
+wire [SDRAM_BANK_W-1:0] addr_bank_w = ram_addr_w[SDRAM_COL_W+3:SDRAM_COL_W+2];
 
 //-----------------------------------------------------------------
 // SDRAM State Machine
@@ -284,13 +295,24 @@ begin
     //-----------------------------------------
     STATE_WRITE0 :
     begin
-        next_state_r = STATE_WRITE1;
+        //next_state_r = STATE_WRITE1;
+
+        next_state_r = STATE_IDLE;
+
+        // Another pending write request (with no refresh pending)
+        //if (!refresh_q && ram_req_w && (ram_wr_w != 4'b0))
+        //begin
+        //    // Open row hit
+        //    if (row_open_q[addr_bank_w] && addr_row_w == active_row_q[addr_bank_w])
+        //        next_state_r = STATE_WRITE0;
+        //end
     end
     //-----------------------------------------
     // STATE_WRITE1
     //-----------------------------------------
     STATE_WRITE1 :
     begin
+        $fatal;
         next_state_r = STATE_IDLE;
 
         // Another pending write request (with no refresh pending)
@@ -461,18 +483,28 @@ else if (state_q == STATE_REFRESH)
 //-----------------------------------------------------------------
 
 reg [SDRAM_DATA_W-1:0] sample_data0_q;
+reg [SDRAM_DATA_W-1:0] sample_data1_q;
 always @ (posedge clk_i or posedge rst_i)
-if (rst_i)
+if (rst_i) begin
     sample_data0_q <= {SDRAM_DATA_W{1'b0}};
-else
+    sample_data1_q <= {SDRAM_DATA_W{1'b0}};
+end
+else begin
     sample_data0_q <= sdram_data_in_w;
+    sample_data1_q <= sdram_data2_in_w;
+end
 
 reg [SDRAM_DATA_W-1:0] sample_data_q;
+reg [SDRAM_DATA_W-1:0] sample_data2_q;
 always @ (posedge clk_i or posedge rst_i)
-if (rst_i)
+if (rst_i) begin
     sample_data_q <= {SDRAM_DATA_W{1'b0}};
-else
+    sample_data2_q <= {SDRAM_DATA_W{1'b0}};
+end
+else begin
     sample_data_q <= sample_data0_q;
+    sample_data2_q <= sample_data1_q;
+end
 
 //-----------------------------------------------------------------
 // Command Output
@@ -484,10 +516,12 @@ if (rst_i)
 begin
     command_q       <= CMD_NOP;
     data_q          <= 16'b0;
+    data2_q         <= 16'b0;
     addr_q          <= {SDRAM_ROW_W{1'b0}};
     bank_q          <= {SDRAM_BANK_W{1'b0}};
     cke_q           <= 1'b0;
     dqm_q           <= {SDRAM_DQM_W{1'b0}};
+    dqm2_q          <= {SDRAM_DQM_W{1'b0}};
     data_rd_en_q    <= 1'b1;
     dqm_buffer_q    <= {SDRAM_DQM_W{1'b0}};
 
@@ -607,6 +641,7 @@ begin
 
         // Read mask (all bytes in burst)
         dqm_q       <= {SDRAM_DQM_W{1'b0}};
+        dqm2_q      <= {SDRAM_DQM_W{1'b0}};
     end
     //-----------------------------------------
     // STATE_WRITE0
@@ -617,13 +652,15 @@ begin
         addr_q          <= addr_col_w;
         bank_q          <= addr_bank_w;
         data_q          <= ram_write_data_w[15:0];
+        data2_q         <= ram_write_data_w[31:16];
 
         // Disable auto precharge (auto close of row)
         addr_q[AUTO_PRECHARGE]  <= 1'b0;
 
         // Write mask
         dqm_q           <= ~ram_wr_w[1:0];
-        dqm_buffer_q    <= ~ram_wr_w[3:2];
+        dqm2_q          <= ~ram_wr_w[3:2];
+        //dqm_buffer_q    <= ~ram_wr_w[3:2];
 
         data_rd_en_q    <= 1'b0;
     end
@@ -632,6 +669,7 @@ begin
     //-----------------------------------------
     STATE_WRITE1 :
     begin
+        $fatal;
         // Burst continuation
         command_q   <= CMD_NOP;
 
@@ -649,13 +687,14 @@ end
 //-----------------------------------------------------------------
 // Record read events
 //-----------------------------------------------------------------
-reg [SDRAM_READ_LATENCY+1:0]  rd_q;
+//reg [SDRAM_READ_LATENCY+1:0]  rd_q;
+reg [SDRAM_READ_LATENCY:0]  rd_q;
 
 always @ (posedge clk_i or posedge rst_i)
 if (rst_i)
-    rd_q    <= {(SDRAM_READ_LATENCY+2){1'b0}};
+    rd_q    <= {(SDRAM_READ_LATENCY+1){1'b0}};
 else
-    rd_q    <= {rd_q[SDRAM_READ_LATENCY:0], (state_q == STATE_READ)};
+    rd_q    <= {rd_q[SDRAM_READ_LATENCY-1:0], (state_q == STATE_READ)};
 
 //-----------------------------------------------------------------
 // Data Buffer
@@ -663,16 +702,17 @@ else
 
 // Buffer upper 16-bits of write data so write command can be accepted
 // in WRITE0. Also buffer lower 16-bits of read data.
-always @ (posedge clk_i or posedge rst_i)
-if (rst_i)
-    data_buffer_q <= 16'b0;
-else if (state_q == STATE_WRITE0)
-    data_buffer_q <= ram_write_data_w[31:16];
-else if (rd_q[SDRAM_READ_LATENCY+1])
-    data_buffer_q <= sample_data_q;
+//always @ (posedge clk_i or posedge rst_i)
+//if (rst_i)
+//    data_buffer_q <= 16'b0;
+//else if (state_q == STATE_WRITE0)
+//    data_buffer_q <= ram_write_data_w[31:16];
+//else if (rd_q[SDRAM_READ_LATENCY+1])
+//    data_buffer_q <= sample_data_q;
 
 // Read data output
-assign ram_read_data_w = {sample_data_q, data_buffer_q};
+//assign ram_read_data_w = {sample_data_q, data_buffer_q};
+assign ram_read_data_w = {sample_data2_q, sample_data_q};
 
 //-----------------------------------------------------------------
 // ACK
@@ -684,9 +724,11 @@ if (rst_i)
     ack_q   <= 1'b0;
 else
 begin
-    if (state_q == STATE_WRITE1)
+    //if (state_q == STATE_WRITE1)
+    if (state_q == STATE_WRITE0)
         ack_q <= 1'b1;
-    else if (rd_q[SDRAM_READ_LATENCY+1])
+    //else if (rd_q[SDRAM_READ_LATENCY+1])
+    else if (rd_q[SDRAM_READ_LATENCY])
         ack_q <= 1'b1;
     else
         ack_q <= 1'b0;
@@ -703,7 +745,9 @@ assign ram_accept_w = (state_q == STATE_READ || state_q == STATE_WRITE0);
 assign sdram_clk_o           = ~clk_i;
 assign sdram_data_out_en_o   = ~data_rd_en_q;
 assign sdram_data_output_o   =  data_q;
+assign sdram_data2_output_o  =  data2_q;
 assign sdram_data_in_w       = sdram_data_input_i;
+assign sdram_data2_in_w      = sdram_data2_input_i;
 
 assign sdram_cke_o  = cke_q;
 assign sdram_cs_o   = command_q[3];
@@ -711,6 +755,7 @@ assign sdram_ras_o  = command_q[2];
 assign sdram_cas_o  = command_q[1];
 assign sdram_we_o   = command_q[0];
 assign sdram_dqm_o  = dqm_q;
+assign sdram_dqm2_o  = dqm2_q;
 assign sdram_ba_o   = bank_q;
 assign sdram_addr_o = addr_q;
 
diff --git a/perip/sdram/sdram_top_apb.v b/perip/sdram/sdram_top_apb.v
index 3bc5b93b..256b9320 100644
--- a/perip/sdram/sdram_top_apb.v
+++ b/perip/sdram/sdram_top_apb.v
@@ -21,12 +21,16 @@ module sdram_top_apb (
   output [12:0] sdram_a,
   output [ 1:0] sdram_ba,
   output [ 1:0] sdram_dqm,
-  inout  [15:0] sdram_dq
+  output [ 1:0] sdram_dqm2,
+  inout  [15:0] sdram_dq,
+  inout  [15:0] sdram_dq2
 );
 
   wire sdram_dout_en;
   wire [15:0] sdram_dout;
+  wire [15:0] sdram_dout2;
   assign sdram_dq = sdram_dout_en ? sdram_dout : 16'bz;
+  assign sdram_dq2 = sdram_dout_en ? sdram_dout2 : 16'bz;
 
   typedef enum [1:0] { ST_IDLE, ST_WAIT_ACCEPT, ST_WAIT_ACK } state_t;
   reg [1:0] state;
@@ -70,10 +74,13 @@ module sdram_top_apb (
     .sdram_cas_o(sdram_cas),
     .sdram_we_o(sdram_we),
     .sdram_dqm_o(sdram_dqm),
+    .sdram_dqm2_o(sdram_dqm2),
     .sdram_addr_o(sdram_a),
     .sdram_ba_o(sdram_ba),
     .sdram_data_input_i(sdram_dq),
+    .sdram_data2_input_i(sdram_dq2),
     .sdram_data_output_o(sdram_dout),
+    .sdram_data2_output_o(sdram_dout2),
     .sdram_data_out_en_o(sdram_dout_en)
   );
 
diff --git a/perip/sdram/sdram_top_axi.v b/perip/sdram/sdram_top_axi.v
index f0fd6fdb..58e37c48 100644
--- a/perip/sdram/sdram_top_axi.v
+++ b/perip/sdram/sdram_top_axi.v
@@ -40,12 +40,16 @@ module sdram_top_axi(
   output [12:0] sdram_a,
   output [ 1:0] sdram_ba,
   output [ 1:0] sdram_dqm,
-  inout  [15:0] sdram_dq
+  output [ 1:0] sdram_dqm2,
+  inout  [15:0] sdram_dq,
+  inout  [15:0] sdram_dq2
 );
 
   wire sdram_dout_en;
   wire [15:0] sdram_dout;
+  wire [15:0] sdram_dout2;
   assign sdram_dq = sdram_dout_en ? sdram_dout : 16'bz;
+  assign sdram_dq2 = sdram_dout_en ? sdram_dout2 : 16'bz;
   sdram_axi #(
     .SDRAM_MHZ(100),
     .SDRAM_ADDR_W(24),
@@ -89,10 +93,13 @@ module sdram_top_axi(
     .sdram_cas_o(sdram_cas),
     .sdram_we_o(sdram_we),
     .sdram_dqm_o(sdram_dqm),
+    .sdram_dqm2_o(sdram_dqm2),
     .sdram_addr_o(sdram_a),
     .sdram_ba_o(sdram_ba),
     .sdram_data_input_i(sdram_dq),
+    .sdram_data2_input_i(sdram_dq2),
     .sdram_data_output_o(sdram_dout),
+    .sdram_data2_output_o(sdram_dout2),
     .sdram_data_out_en_o(sdram_dout_en)
   );
 
diff --git a/src/SoC.scala b/src/SoC.scala
index e82b3ad1..ea1662f9 100644
--- a/src/SoC.scala
+++ b/src/SoC.scala
@@ -43,7 +43,7 @@ class ysyxSoCASIC(implicit p: Parameters) extends LazyModule {
   val lmrom = LazyModule(new AXI4MROM(AddressSet.misaligned(0x20000000, 0x1000)))
   val sramNode = AXI4RAM(AddressSet.misaligned(0x0f000000, 0x2000).head, false, true, 4, None, Nil, false)
 
-  val sdramAddressSet = AddressSet.misaligned(0xa0000000L, 0x2000000)
+  val sdramAddressSet = AddressSet.misaligned(0xa0000000L, 0x4000000)
   val lsdram_apb = if (!Config.sdramUseAXI) Some(LazyModule(new APBSDRAM (sdramAddressSet))) else None
   val lsdram_axi = if ( Config.sdramUseAXI) Some(LazyModule(new AXI4SDRAM(sdramAddressSet))) else None
 
diff --git a/src/device/SDRAM.scala b/src/device/SDRAM.scala
index 2fa845bc..2d858af5 100644
--- a/src/device/SDRAM.scala
+++ b/src/device/SDRAM.scala
@@ -10,7 +10,7 @@ import org.chipsalliance.cde.config.Parameters
 import freechips.rocketchip.diplomacy._
 import freechips.rocketchip.util._
 
-class SDRAMIO extends Bundle {
+class SDRAMIO(databits: Int = 16) extends Bundle {
   val clk = Output(Bool())
   val cke = Output(Bool())
   val cs  = Output(Bool())
@@ -19,8 +19,8 @@ class SDRAMIO extends Bundle {
   val we  = Output(Bool())
   val a   = Output(UInt(13.W))
   val ba  = Output(UInt(2.W))
-  val dqm = Output(UInt(2.W))
-  val dq  = Analog(16.W)
+  val dqm = Output(UInt((databits / 8).W))
+  val dq  = Analog(databits.W)
 }
 
 class sdram_top_axi extends BlackBox {
@@ -28,7 +28,7 @@ class sdram_top_axi extends BlackBox {
     val clock = Input(Clock())
     val reset = Input(Bool())
     val in = Flipped(new AXI4Bundle(AXI4BundleParameters(addrBits = 32, dataBits = 32, idBits = 4)))
-    val sdram = new SDRAMIO
+    val sdram = new SDRAM32IO
   })
 }
 
@@ -37,16 +37,16 @@ class sdram_top_apb extends BlackBox {
     val clock = Input(Clock())
     val reset = Input(Bool())
     val in = Flipped(new APBBundle(APBBundleParameters(addrBits = 32, dataBits = 32)))
-    val sdram = new SDRAMIO
+    val sdram = new SDRAM32IO
   })
 }
 
 class sdram extends BlackBox {
-  val io = IO(Flipped(new SDRAMIO))
+  val io = IO(Flipped(new SDRAM32IO))
 }
 
-class sdramChisel extends RawModule {
-  val io = IO(Flipped(new SDRAMIO))
+class MT48LC16M16A2 extends RawModule {
+  val io = IO(Flipped(new SDRAMIO(16)))
   val (rowWidth, colWidth, bankWidth) = (io.a.getWidth, 9, io.ba.getWidth)
   val addrWidth = rowWidth + colWidth + bankWidth
   withClock(io.clk.asClock) {
@@ -70,6 +70,28 @@ class sdramChisel extends RawModule {
   }
 }
 
+class SDRAM32IO extends SDRAMIO(16) {
+  val dqm2 = Output(UInt(2.W))
+  val dq2  = Analog(16.W)
+}
+
+class sdramChisel extends RawModule {
+  val io = IO(Flipped(new SDRAM32IO))
+  val chips = List.fill(2)(Module(new MT48LC16M16A2))
+  chips.map(_.io.clk := io.clk)
+  chips.map(_.io.cke := io.cke)
+  chips.map(_.io.cs  := io.cs)
+  chips.map(_.io.ras := io.ras)
+  chips.map(_.io.cas := io.cas)
+  chips.map(_.io.we  := io.we)
+  chips.map(_.io.a   := io.a)
+  chips.map(_.io.ba  := io.ba)
+  chips(0).io.dqm <> io.dqm
+  chips(1).io.dqm <> io.dqm2
+  chips(0).io.dq  <> io.dq
+  chips(1).io.dq  <> io.dq2
+}
+
 class AXI4SDRAM(address: Seq[AddressSet])(implicit p: Parameters) extends LazyModule {
   val beatBytes = 4
   val node = AXI4SlaveNode(Seq(AXI4SlavePortParameters(
@@ -85,7 +107,7 @@ class AXI4SDRAM(address: Seq[AddressSet])(implicit p: Parameters) extends LazyMo
   lazy val module = new Impl
   class Impl extends LazyModuleImp(this) {
     val (in, _) = node.in(0)
-    val sdram_bundle = IO(new SDRAMIO)
+    val sdram_bundle = IO(new SDRAM32IO)
 
     val msdram = Module(new sdram_top_axi)
     msdram.io.clock := clock
@@ -107,7 +129,7 @@ class APBSDRAM(address: Seq[AddressSet])(implicit p: Parameters) extends LazyMod
   lazy val module = new Impl
   class Impl extends LazyModuleImp(this) {
     val (in, _) = node.in(0)
-    val sdram_bundle = IO(new SDRAMIO)
+    val sdram_bundle = IO(new SDRAM32IO)
 
     val msdram = Module(new sdram_top_apb)
     msdram.io.clock := clock
-- 
2.43.0

